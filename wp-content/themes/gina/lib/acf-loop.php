<?php

// Useful for when you need to know information about a module before rendering
// E.G. a related content module.

// // Check if page builder is being used
// if (have_rows('acf_field_page_content') ) :

//   while( have_rows('acf_field_page_content') ) : the_row();

//     // Check related posts is being used
//     if(get_row_layout() == 'layout') :

//     endif;
//   endwhile; // page builder loop

// endif;
